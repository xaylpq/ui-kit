import React from "react";

/* todo
    * add defaults design states
    * add event handlers
 */

const defaultDesingStates = [
    text = 'khuy'
];

export const Button = (props) => (

    <button color="blue" shadowSize={2}>
        {text}
    </button>
)
